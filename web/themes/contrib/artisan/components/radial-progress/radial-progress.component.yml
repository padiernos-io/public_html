$schema: https://git.drupalcode.org/project/drupal/-/raw/HEAD/core/assets/schemas/v1/metadata.schema.json
name: Radial Progress
description: A radialprogress component based on DaisyUI
props:
  type: object
  properties:
    value:
      title: Value
      type: integer
      minimum: 0
      maximum: 100
    color:
      title: Color
      type: string
      enum:
        - neutral
        - primary
        - secondary
        - accent
        - info
        - success
        - warning
        - error
