{%
  set classes = [
    'media',
    'media--type-' ~ bundle|clean_class,
    not published ? 'media--unpublished',
    view_mode ? 'media--view-mode-' ~ view_mode|clean_class,
  ]
%}
<div {{ attributes.addClass(classes)}}>
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
