<?php

/**
 * @file
 * Twig Placeholders module.
 */

/**
 * Implements hook_theme().
 *
 * Defines the theme hook for Twig Placeholders.
 *
 * @phpstan-ignore-next-line
 */
function twig_placeholders_theme(): array {
  return [
    'twig_placeholders_video' => [
      'variables' => [
        'attributes' => [],
        'files' => [],
      ],
    ],
  ];
}
