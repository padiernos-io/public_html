services:
  patreon.api:
    class: Drupal\patreon\PatreonService
    arguments: ['@path.current', '@config.factory', '@logger.factory', '@messenger', '@entity_type.manager', '@request_stack', '@state']
