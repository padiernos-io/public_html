/**
 * @file
 * GLightbox Media Video integration.
 */

(function($) {
  Drupal.behaviors.glightboxMediaVideo = {
    attach: function (context, settings) {
      // Glightbox initialized in main glightbox.js file.
    }
  };
})(jQuery);
