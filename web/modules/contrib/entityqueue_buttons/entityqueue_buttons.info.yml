name: EntityQueue Buttons
type: module
description: 'Puts buttons on nodes making it fast to 1) add the node to an entityqueue or 2) remove it from an entity queue. Provides a great settings page to manage EntityQueue assignments per content type.'
package: Entityqueue
core_version_requirement: ^10 || ^11
configure: entityqueue_buttons.settings
dependencies:
  - drupal:node
  - entityqueue:entityqueue
# Information added by Drupal.org packaging script on 2025-10-07
version: '1.1.0'
project: 'entityqueue_buttons'
datestamp: 1759833077
